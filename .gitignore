# 1. 보안 파일 (절대 올리면 안 됨!)
.env
# 2. 파이썬 컴파일/캐시 파일
__pycache__/
*.py[cod]
# 3. 가상환경 폴더 (만약 가상환경을 사용햇다면 내 컴퓨터 설정이므로 올리지 않음 - 그러나 우리는 conda를 사용햇으므로 불필요)
venv/
env/
.venv/

# 4. OS 관련 잡동사니
.DS_Store

# 5. IDE 설정 파일
.vscode/
.idea/

# 6. 생성된 데이터 파일 (선택사항: 로컬에서만 볼 거면 추가)
latest_trend.txt